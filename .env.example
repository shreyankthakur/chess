# Example environment variables for local development and CI
# Copy this to `.env` (do NOT commit secrets to git)

# Backend (Django)
DJANGO_SECRET_KEY=replace_me_with_a_strong_secret
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Frontend / Deployment
VITE_BASE_API=http://localhost:8000/api

# CI / GitHub secrets (do NOT store these locally in repo)
# RENDER_API_KEY=...
# RENDER_SERVICE_ID=...
# VERCEL_TOKEN=...
# VERCEL_ORG_ID=...
# VERCEL_PROJECT_ID=...
